# Dashboard Placeholder

The initial MVP exposes the discovery workflow through FastAPI endpoints.

Recommended next step for the dashboard:

- list candidate reports
- filter by seed category and risk flags
- show score breakdown
- render trend charts from stored `trend_snapshots`
