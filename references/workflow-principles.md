# Workflow Principles

- Prefer deterministic, typed pipeline stages over agentic loops.
- Use LLMs for generation and interpretation, not final scoring authority.
- Keep external API calls cached and replayable.
- Make reruns idempotent where practical.
